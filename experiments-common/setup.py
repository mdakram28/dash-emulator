#!/usr/bin/env python3

from setuptools import find_packages, setup

setup(name='experiments-common',
      version='0.1.0-dev',
      description='Library for common experiment utlities',
      author='Akram AnsarI',
      author_email='mdakram28@gmail.com',
      packages=find_packages(),
      scripts=[],
      # install_requires=requirements,
      )
